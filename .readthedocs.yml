version: 2

build:
  on: "ubuntu-22.04"
  tools:
    python: "3.10"

python:
  install:
    - method: pip
      path: .
    - requirements: requirements/docs.txt

sphinx:
  configuration: docs/conf.py

formats: all
